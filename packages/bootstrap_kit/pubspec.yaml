name: bootstrap_kit
description: App startup orchestration — a guarded crash boundary and a retryable startup-job engine.
version: 0.0.3
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  bloc: ^9.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint_kit:
    path: ../lint_kit
