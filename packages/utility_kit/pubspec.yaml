name: utility_kit
description: Shared, framework-level utility helpers for the Fatiharge monorepo.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  bloc: ^9.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint_kit:
    path: ../lint_kit
