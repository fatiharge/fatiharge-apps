# Dart / Flutter
.dart_tool/
.packages
build/
**/doc/api/
coverage/
*.log

# FVM (keep .fvmrc, ignore the local SDK symlink/cache)
.fvm/

# IDE
/.idea
*.iml
.vscode/

# OS
.DS_Store

# Mason — lock file pins local-path bricks by ABSOLUTE path (machine-specific),
# so it is not portable across machines/CI. Do not commit it.
mason-lock.json

# Misc
/.config

# Personal local git helper (not part of the repo)
/scripts/gitp.py
